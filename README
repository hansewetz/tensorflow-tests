(1) make vcreate                       # creates virtual env for python and installs a numbert of modules
(2) cd env; . ./setenv.sh; cd ..       # source environment - will activate the python virtual env
